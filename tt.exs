# Voka.Deepl.trans_word("retourner", "FR", "ES")
# |> IO.inspect(label: "translated")

Voka.OSDict.fra_to_spa("retourner")
|> IO.inspect(label: "retourner")

Voka.OSDict.fra_to_spa("table")
|> IO.inspect(label: "table")

Voka.OSDict.fra_to_spa("mort")
|> IO.inspect(label: "mort")
